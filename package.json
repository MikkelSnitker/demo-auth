{
  "name": "oauth-final-strict",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build &&  node --inspect -r ./mock.js dist/index",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.0.0"
  }
}
